//swaping operation
#include <stdio.h>
int main() {
  double first, second, temp;             
  printf("Enter 2 numbers: ");
  scanf("%lf %lf" , &first, &second);
  temp = first;
  first = second;
  second = temp;
  printf("%.2lf %.2lf, first, second);
         return 0;
     
         }         
